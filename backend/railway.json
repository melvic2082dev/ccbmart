{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '[STARTUP] running migrate' && (npx prisma migrate deploy || echo '[STARTUP] migrate failed, continuing') && echo '[STARTUP] launching node' && exec node src/server.js",
    "healthcheckPath": "/api/ping",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
